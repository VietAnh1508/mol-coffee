// Default fallback allowance amount (VND) if DB-configured rates are unavailable
export const LUNCH_ALLOWANCE = 30_000;

// Vietnam operates on UTC+7; used for payroll month boundary calculations
export const VN_TIMEZONE_OFFSET_MINUTES = 7 * 60;
